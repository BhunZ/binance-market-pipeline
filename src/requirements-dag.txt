# Installed into the Airflow containers on top of the base image. Deliberately short: the DAGs
# need an HTTP client, a dataframe, a Parquet writer, an S3 client, a Postgres driver, DuckDB to
# read Parquet in place, and dbt to build the models. Anything heavier belongs in a task that
# runs elsewhere, not in an image the scheduler has to start.
requests>=2.32
pandas>=2.2
pyarrow>=15.0
boto3>=1.34
duckdb>=1.0
psycopg[binary]>=3.1
dbt-postgres>=1.8
